import { styled } from '@mui/styles';

export const Nav = styled('nav')({
  fontSize: 24,
  padding: 20,
  width: 150,
  height: 280,
});

export const NavItem = styled('div')({
  color: '#5cb0d4',
  textDecoration: 'none',
});
