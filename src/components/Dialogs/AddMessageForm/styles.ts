import styled from 'styled-components';

export const Wrapper = styled.div`
  .form-body {
    display: flex;
    flex-direction: column;
    width: 60%;
    margin-top: 1rem;
  }
  .form-body button {
    width: 20%;
  }
`;
